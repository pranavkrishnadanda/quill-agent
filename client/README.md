# job-agent-client-ts

Shared TypeScript client for the Job Agent — talks to the local verification server, wraps Anthropic's API, and provides framework-agnostic form-field detection + resume schema. Consumed by the Chrome extension (and any future CLI/web-app).
